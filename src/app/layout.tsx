// Minimal root layout — all pages live under [locale]/layout.tsx
// This file satisfies Next.js App Router requirements for a root layout
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
